This feature is used only to run findbugs on all the Java classes we are interested in,
that is, main Xtraitj plug-ins and examples.

We can't use the site feature because we are not publish example projects in the p2 repositories.
(Examples are to be imported in the workspace using our wizard).